<?xml version="1.0" encoding="UTF-8"?>
<document xmlns="http://maven.apache.org/XDOC/2.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/XDOC/2.0
    http://maven.apache.org/xsd/xdoc-2.0.xsd">
  <properties>
    <title>Matlab</title>
    <author email="michael-schnieders@uiowa.edu">Michael J. Schnieders</author>
  </properties>
  <body>
    <section name="Matlab Integration">
      <p>Force Field X commands can be run within Matlab.</p>
      <img src="images/matlab.png" alt="Matlab"/>
      <subsection name="Setting up Force Field X with Matlab.">
        <p>
          The instructions below are available in the file "ffx/matlab/SetupFFX.m". Please follow these
          instructions to configure Java and Force Field X within the Matlab environment.
          <br/>
        </p>
        <p>
          <b>Loading the Java SDK into Matlab</b>
          <br/>
          As of Matlab 2026b, both Matlab and Force Field X support JDK/JRE version 25.
          Please install JDK 25 from Oracle or from Adopt Open JDK:
          <br/>
            <a href="https://www.oracle.com/java/technologies/downloads/#java25">Oracle JDK 25</a>
          <br/>
          - or -
          <br/>
            <a href="https://adoptium.net/temurin/releases?version=25">Adopt Open JDK 25</a>
          <br/>
          Matlab can be configured to use this JDK use the 'jenv' command:
          <br/>
        </p>
          <source>jenv("/Library/Java/JavaVirtualMachines/jdk-25.0.3+9/Contents/Home")</source>
        <p>
          To check the current Matlab Java version, first execute a Java command to force loading of the Java Virtual Machine.
        </p>
        <source>java.lang.String('Load Java.')</source>
        <p>
          Check the current JDK version:
        </p>
        <source>version -java
 ans = 'Java 25.0.3+9-LTS with Eclipse Adoptium OpenJDK 64-Bit Server VM mixed mode, emulated-client, sharing'</source>
        <p>
            <b>Adding Force Field X to the Matlab Classpath</b>
          <br/>
          Once Matlab is running on JRE/JDK 25, the FFX jar files need to be added
          to the "static" Matlab classpath as documented
            <a href="https://www.mathworks.com/help/matlab/matlab_external/java-class-path.html">here.</a>
          <br/>
          The SetupFFX.m script will configure the classpath by running the following commands:
        </p>
          <source>
% Add JAR files to "javaclasspath.txt" in the Matlab pref directory.
cd(prefdir)
fid = fopen('javaclasspath.txt','wt');
for n = 1:length(jarfiles)
  file = jarfiles(n);
  if (length(file.name) > 4)
    jar = ffxlib + file.name;
    fprintf(fid, '%s\n', jar);
  end
end
fclose(fid);</source>
        <p>
          Matlab must be restarted for the new static classpath to take effect.
        </p>
        <p>
          <b>Configure Java Start-Up Options</b>
          <br/>
          FFX requires the incubating Vector extensiuons be enabled. This is configured using a Matlab Java
            start-up options file as documented
            <a href="https://www.mathworks.com/help/matlab/matlab_env/java-opts-file.html">here.</a>
          <br/>
          The SetupFFX.m script will configure the start-up options by running the following comamnds:
        </p>
          <source>
% Configure Java Start-Up Options to enable Vector Calculations
startupDir = matlabroot + "/bin/" + computer('arch');
cd(startupDir);
fid = fopen('java.opts','wt');
fprintf(fid, '%s\n', '--add-modules=jdk.incubator.vector');
fclose(fid);</source>
          <p>
          Matlab must be restarted for the new start-up options to take effect.
        </p>
      </subsection>
      <subsection name="Running Force Field X Commands">
        <p>Once Matlab is running on Java v. 25, an example using Force Field X commands is given in the script
          "ffx/matlab/ForceFieldX.m".
        <br/>
          Matlab Java syntax documentation is available
          <a href="https://www.mathworks.com/help/matlab/using-java-libraries-in-matlab.html">here.</a>
        </p>
          <source>
import java.util.ArrayList
import ffx.utilities.FFXBinding
import ffx.potential.commands.Energy

% Set the FFX_HOME variable to the root FFX directory.

FFX_HOME = "/Users/mjschnie/Data/ffx-project/forcefieldx";

% Script input Arguments
A = ArrayList;
A.add("-m");
A.add(FFX_HOME + "/examples/dhfr.xyz");

% Create a Binding
binding = FFXBinding;
binding.setVariable("args", A);

% Run the Energy command
command = Energy;
command.setBinding(binding);
command.run();

% Get back the ForceFieldEnergy instance
forceFieldEnergy = command.forceFieldEnergy;
forceFieldEnergy.toString()</source>
      </subsection>
    </section>
  </body>
</document>
