import ffx.potential.PotentialEnergy;
import ffx.potential.bonded.Atom;

String filename = "examples/dhfr.xyz";
open filename;

Atom[] atoms = active.getAtomArray();
atoms[0].setApplyLambda(true);

energy = new PotentialEnergy(active);
energy.setSoftCoreLambda(0.5);
energy.setElectrostaticsLambda(0.5);

energy.energy(false, true);

